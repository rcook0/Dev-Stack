{
  "name": "Dev Stack Workspace",
  "image": "ghcr.io/rcook0/dev-stack:dev",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "remoteUser": "root",
  "remoteEnv": {
    "PATH": "/opt/venv/bin:${containerEnv:PATH}"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-vscode.cpptools",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "postCreateCommand": "echo \"alias gpull='git fetch origin && git pull'\" >> ~/.bashrc && echo \"gpush() { git add . && git commit -m \\\"${1:-update}\\\" && git push; }\" >> ~/.bashrc && .github/scripts/sanity.sh || true"
}
